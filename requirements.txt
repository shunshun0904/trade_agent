# Runtime dependencies (Lambda). boto3 is provided by the Lambda runtime but is
# pinned here so local runs and tests match.
anthropic>=1.0.0,<2.0.0
pydantic>=2.7,<3.0
requests>=2.31,<3.0
PyYAML>=6.0,<7.0
boto3>=1.34,<2.0
